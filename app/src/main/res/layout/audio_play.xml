<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:minWidth="200dp"
    android:padding="8dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/audio_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/audioPlayPause"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:contentDescription="@string/play_and_pause_audio"
            android:src="@drawable/pause_24px"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/onPrimary" />

        <SeekBar
            android:id="@+id/audioSeekBar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:max="100"
            android:progress="0"
            android:progressBackgroundTint="@color/onPrimary"
            android:progressTint="@color/primary"
            android:thumbTint="@color/primary"
            app:layout_constraintBottom_toBottomOf="@id/audioPlayPause"
            app:layout_constraintEnd_toStartOf="@id/audioDuration"
            app:layout_constraintStart_toEndOf="@id/audioPlayPause"
            app:layout_constraintTop_toTopOf="@id/audioPlayPause" />

        <TextView
            android:id="@+id/audioDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/onPrimary"
            android:textSize="14sp"
            app:layout_constraintBottom_toBottomOf="@id/audioPlayPause"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/audioPlayPause"
            tools:text="0:21" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <include
        android:id="@+id/captions_include"
        layout="@layout/captions"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/audio_container" />
</androidx.constraintlayout.widget.ConstraintLayout>